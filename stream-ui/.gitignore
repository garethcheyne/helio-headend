# dependencies
node_modules

# next.js
/.next/
/out/

# production
/build
dist

# misc
*.tsbuildinfo
next-env.d.ts

# env — ignore all real env files (may hold secrets), keep the *.example templates
.env
.env.*
!.env.example
!.env.local.example
!.env.production.example

# sqlite runtime data (the live DB). NOTE: seed/ is committed on purpose.
/data/
